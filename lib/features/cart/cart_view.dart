import 'package:flutter/material.dart';
import 'package:zara/features/cart/cart_body.dart';

class CartView extends StatelessWidget {
  const CartView({super.key});

  @override
  Widget build(BuildContext context) {
    return CartBody();
  }
}